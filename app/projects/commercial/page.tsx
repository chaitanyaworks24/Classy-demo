import Link from 'next/link'; import {projects} from '@/data/projects';
export default function CommercialProjects(){const items=projects.filter(p=>p.category==='Commercial');return <main className="page"><section className="page-intro"><div><div className="eyebrow">COMMERCIAL</div><h1>Commercial Interiors</h1></div><p>Workplaces, hospitality and retail environments designed to support people, brand and movement.</p></section><section className="project-grid">{items.map(p=><Link href={`/projects/${p.slug}`} className="project-tile" key={p.slug}><div className="project-image"><img src={p.image} alt={p.title}/></div><h3>{p.title}</h3><p>{p.homeType} · {p.style}</p></Link>)}</section></main>}
