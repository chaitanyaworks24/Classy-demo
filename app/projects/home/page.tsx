import Link from 'next/link'; import {projects} from '@/data/projects';
export default function HomeProjects(){const items=projects.filter(p=>p.category==='Residential');return <main className="page"><section className="page-intro"><div><div className="eyebrow">RESIDENTIAL</div><h1>Home Interiors</h1></div><p>Explore kitchens, living rooms, bedrooms and the details that turn a house into a considered home.</p></section><section className="project-grid">{items.map(p=><Link href={`/projects/${p.slug}`} className="project-tile" key={p.slug}><div className="project-image"><img src={p.image} alt={p.title}/></div><h3>{p.title}</h3><p>{p.homeType} · {p.style}</p></Link>)}</section></main>}
