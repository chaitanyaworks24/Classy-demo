import {notFound} from 'next/navigation'; import Link from 'next/link'; import {blogs} from '@/data/blogs';
export function generateStaticParams(){return blogs.map(b=>({slug:b.slug}))}
export default async function BlogDetail({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const b=blogs.find(x=>x.slug===slug);if(!b)notFound();return <main className="article"><div className="article-head"><div className="eyebrow">{b.category}</div><h1>{b.title}</h1><p>{b.excerpt}</p></div><img className="article-hero" src={b.image} alt=""/><div className="article-body"><p>Interior projects become easier when the important decisions are made in the right order. Start with the way the space needs to work, then decide what deserves visual emphasis.</p><h2>Begin with the brief</h2><p>List the everyday frustrations, storage needs, people using the space and the feeling you want when you walk through the door. This becomes a useful filter for every later decision.</p><h2>Make the investment intentional</h2><p>Separate must-haves from nice-to-haves. A clear scope lets you decide where to spend more on the things you touch and see every day, and where a simpler solution is perfectly adequate.</p><h2>Let materials support the architecture</h2><p>When colour, texture and joinery are chosen as a system, a room can feel layered without becoming busy.</p><Link className="text-link" href="/contact">Talk to a Designer →</Link></div></main>}
