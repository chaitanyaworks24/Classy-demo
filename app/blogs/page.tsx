import Link from 'next/link'; import {blogs} from '@/data/blogs';
export default function Blogs(){return <main className="page"><section className="page-intro"><div><div className="eyebrow">JOURNAL</div><h1>Ideas for better spaces.</h1></div><p>Practical notes on planning, materials, storage and the decisions behind a well-considered interior.</p></section><section className="blog-grid">{blogs.map(b=><Link href={`/blogs/${b.slug}`} className="blog-card" key={b.slug}><img src={b.image} alt=""/><div className="blog-copy"><div className="eyebrow">{b.category}</div><h2>{b.title}</h2><p>{b.excerpt}</p><span>Read article →</span></div></Link>)}</section></main>}
