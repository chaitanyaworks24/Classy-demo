import Link from 'next/link'; import {ArrowRight} from 'lucide-react'; import {homeServices} from '@/data/services';
export default function HomeServices(){return <main className="page"><section className="page-intro"><div><div className="eyebrow">RESIDENTIAL</div><h1>Home Interiors</h1></div><p>From compact apartments to larger homes, we build a consistent design language around how you live.</p></section><div className="service-grid service-grid-large">{homeServices.map(x=><article className="service-card" key={x.slug}><img src={x.image} alt={x.title}/><div className="service-card-copy"><div className="eyebrow">{x.eyebrow}</div><h2>{x.title}</h2><p>{x.desc}</p><Link href={`/services/home/${x.slug}`}>Explore this service <ArrowRight size={15}/></Link></div></article>)}</div></main>}
