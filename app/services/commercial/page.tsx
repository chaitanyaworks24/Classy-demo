import Link from 'next/link'; import {ArrowRight} from 'lucide-react'; import {commercialServices} from '@/data/services';
export default function CommercialServices(){return <main className="page"><section className="page-intro"><div><div className="eyebrow">COMMERCIAL</div><h1>Commercial Interiors</h1></div><p>Design for offices, hospitality and retail — where brand, function and experience need to coexist.</p></section><div className="service-grid service-grid-large">{commercialServices.map(x=><article className="service-card" key={x.slug}><img src={x.image} alt={x.title}/><div className="service-card-copy"><div className="eyebrow">COMMERCIAL INTERIORS</div><h2>{x.title}</h2><p>{x.desc}</p><Link href={`/services/commercial/${x.slug}`}>Explore this service <ArrowRight size={15}/></Link></div></article>)}</div></main>}
